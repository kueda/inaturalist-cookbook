# windshaft load balancer
<%- node.run_state["windshaft_load_balancers"].each do |server| -%>
# <%= server["name"] %>
-A INPUT -p tcp -s <%= server["ipaddress"] %> --dport 4000 -j ACCEPT
<%- end -%>

